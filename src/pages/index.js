
import Link from 'next/link';
import DropdownMenu from '../components/DropdownMenu';


const Home = ({ dropdownMenu }) => {
  return (
    <DropdownMenu>
  
    </DropdownMenu>
  );
};

export default Home;